// import PropTypes from 'prop-types';

export const Filter = () => {
    return ( <label>
          Find contacts by name
          <input
            type="text"
            // placeholder="Enter login"
          />
        </label>);
}